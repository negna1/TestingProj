

import UIKit

extension CheckBox {
    struct TermTextLabelConsts {
        struct Color {
            static let defaultTextColor = UIColor.gray
            static let externalLinkTextColor = UIColor.gray
            static let disabledTextColor = UIColor.gray
            static let errorTextColor = UIColor.gray
        }
        
        struct Metric {
            static let numberOfLinesWhenWithoutTerm = 1
            static let numberOfLinesWhenWithTerm = 2
        }
    }
}
