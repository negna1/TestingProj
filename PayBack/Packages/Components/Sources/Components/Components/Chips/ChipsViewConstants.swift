
import UIKit

struct ChipsViewConstants {
    static let minimumSpacing = Spacing.S.floatValue
    static let contentInset: UIEdgeInsets = UIEdgeInsets(top: .zero, left: Spacing.M.floatValue, bottom: .zero, right: .zero)
    static let charLength: CGFloat = 7
    static let cellPadding: CGFloat = 40 // 2 * Spacing.L.FloatValue
    static let checkboxWidth = Spacing.XL.floatValue
    static let cellHeight: CGFloat = 30
}
